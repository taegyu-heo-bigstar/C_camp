과제

# 포인터 제대로 다뤕모기

## 다음의 함수를 제작하라

1. "void swap(int *a, int *b)"로 선언된 값 스왑 함수제작하기
2. "int *val"을 멤버로 포함하는 구조체 포인터를 사용하여
    위와 유사한 형태의 값 스왑 함수 제작하기
3-1. 프로세스와 가상 메모리에 대하여 조사하기
3-2. string.h에서 문자열을 다루는 함수(복사 포함) 3가지 이상 조사하고,
문제가 생기는 상황과 해결방안을 제시하기.